# =============================================================================
# Production Environment
# =============================================================================
# This directory can be used for environment-specific overrides.
# The actual configuration is in the root module with variables.

# To use this environment:
# cd environments/prod
# terraform init
# terraform apply

# Or use Terraform Cloud workspace with the root module directly.
